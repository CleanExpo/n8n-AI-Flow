{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "ls -la && cat package.json | head -20 && npm install --verbose",
  "regions": ["iad1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}